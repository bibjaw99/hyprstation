# just a hyprland rice

```sh
mkdir -p ~/github && (cd ~/github/hyprstation || git clone https://github.com/bibjaw99/hyprstation ~/github/hyprstation) && [ -f ~/github/hyprstation/install_scripts/install.sh ] && bash ~/github/hyprstation/install_scripts/install.sh || echo "Install script not found."
```
